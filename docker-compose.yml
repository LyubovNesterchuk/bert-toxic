services:
  toxicguard:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: toxicguard

    ports:
      - "8501:8501"

    volumes:
      - ./models:/app/models

    restart: unless-stopped

    environment:
      - PYTHONUNBUFFERED=1